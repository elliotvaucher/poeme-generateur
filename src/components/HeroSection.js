import React from 'react';
import { Typography } from '@mui/material';

const HeroSection = () => (
  <div>
    <Typography variant="h2" gutterBottom>
      Ton poème en un clic
    </Typography>
  </div>
);

export default HeroSection;
